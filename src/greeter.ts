function greeter(person: string) {
    return "Hello, " + person;
}

let user = "Jane User";
console.log(greeter(user));

const a: number = 3;
const b: string = "abc";
const c: null = null;
const d: string = "test";
